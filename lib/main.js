//#region import
const {
  setConfig,
  resetConfig,
  getConfig
} = require('./config');
const Base = require('./class/Base');
//#endregion

module.exports = {
  Base,
  setConfig,
  resetConfig,
  getConfig,
};